# Code review — uncommitted changes (cloak self-heal + profile onboard) — 2026-06-08

Independent review (opus code-reviewer) + self gap-analysis. Verdict ban đầu: **REQUEST CHANGES**.
Tất cả HIGH/MEDIUM đã fix + test; LOW xử lý phần lớn. **276 tests pass, ruff 0 lỗi mới, smoke thật PASS.**

| # | Sev | Vấn đề | Trạng thái |
|---|---|---|---|
| 1 | HIGH | `is_cloudflare_challenge` body-scan chạy vô điều kiện → grok trả lời 200 *nhắc tới* Cloudflare bị flag nhầm, vứt câu trả lời thật | ✅ **FIXED** (fix A): `200 && !is_cf → return False`; tách strict-token vs prose-marker |
| 2 | HIGH | statsig lưu `cf_clearance`/statsig dưới `CHROME_PROFILE_USED_GROK or "Default"` nhưng `get_grok_cookies_cached` đọc **chosen profile** → self-heal không thấy cookie vừa earn | ✅ **FIXED**: `_capture_profile_name()` (env→chosen→legacy→Default) + test |
| 3 | HIGH/LOW | predicate 403: (a) **false-positive** auth-403 JSON sau Cloudflare (perplexity/grok đều sau CF có `cf-ray`); (b) miss 403 `text/html` không `cf-mitigated` | ✅ **FIXED (a)**: bỏ heuristic `403+cf+non-html`, chỉ tin `cf-mitigated`/503/body-token. ⚠️ **(b) nhận-có-điều-kiện**: perplexity dựa `cf-mitigated` header (không đọc body SSE stream); CF managed challenge luôn gửi `cf-mitigated` |
| 4 | MED | `get_first_valid([chosen])` fallback sang profile KHÁC khi chosen chưa cache | ✅ **FIXED** (fix B): chosen → `get_profile_entry(chosen)` only (như gemini) + test |
| 5 | MED | "temporary" auto-pick re-scan + re-warn mỗi cold call | ✅ **FIXED**: `_note_active_profile` de-dup per process |
| 6 | MED | `headless=False` cứng → vỡ trên server Linux không `$DISPLAY` | ✅ **FIXED**: `_default_headless()` (Linux-no-display→headless; env `GROK_STATSIG_HEADLESS` override) + test |
| 7 | MED | `GROK_MAJOR` frozen lúc import → comment "self-corrects" hơi quá | ◻️ **NOTED**: `cloakbrowser_binary_major` lru_cached ⇒ UA & emulation cùng 1 giá trị frozen, KHÔNG drift; tự sửa khi restart. Không đổi code |
| 8 | LOW | docstring lỗi thời ("scans every profile" / "Chrome145") | ✅ grok/cookies module docstring fixed; ◻️ grok/client header để nguyên (145 = giá trị thật hiện tại) |
| 9 | LOW | `_note_active_profile` print stderr trên MCP stdio | ✅ **FIXED**: route qua `logging` (kênh log, không phải stdout protocol) |
| 10 | LOW | `pick_rnet_emulation_name` dead code | ✅ **FIXED**: rút gọn |

## Verification
- `pytest tests/ -q` → **276 passed** (+ test_statsig.py mới; +test predicate true-negative/strict; +test chosen-ignores-other-cache).
- `ruff check` (8 file đổi) → chỉ 2 **E402 pre-existing** ở `perplexity/client.py` (import sau `logging.basicConfig`, có từ HEAD, net delta 0 — giữ nguyên theo surgical).
- Smoke thật: grok-answer-about-CF(200)→False; auth-403-behind-CF→False; cf-mitigated/strict-token/503→True; headful trên macOS; `_capture_profile_name`→Default.

## Files chạm (round review)
`cloak.py` (predicate, pick_rnet), `grok/statsig.py` (profile key + headless), `perplexity/client.py` (_refresh_cookies chosen-aware + imports), `cookies.py` (logging+dedupe), `grok/cookies.py` (docstring),
tests: `test_cloak.py`, `test_client.py` (isolate config + bỏ patch legacy), `test_statsig.py` (mới), `test_cookies.py`/`test_grok_cookies_cache.py` (round trước).
